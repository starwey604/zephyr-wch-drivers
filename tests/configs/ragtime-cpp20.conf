# SPDX-License-Identifier: Apache-2.0

# Workspace compatibility: the pinned OnePID module exports a C++20 target.
# The WCH driver and test sources themselves are C, not C++.
CONFIG_CPP=y
CONFIG_STD_CPP20=y
